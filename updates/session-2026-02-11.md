# Decoded Chart Markers & Animation Polish - February 11, 2026

Added user join date markers to the library growth chart and refined the box intro animation timing.

## Changes:
- Added subtle vertical marker lines on the library growth chart showing when each user joined the server
- Markers use CSS-only hover (no React state) to avoid re-triggering chart animations
- Hover tooltip shows avatar thumbnails and usernames, with stacking for same-day joins
- Reworked box-to-title transition: box slides upward 0.6s before title enters from below
- Reduced bit fountain count from 1000 to 99 (desktop) and 350 to 35 (mobile)
- Reduced title entrance delay by 10%

## Technical Details:
- InteractiveChart: Added `userJoins` prop with Recharts `ReferenceLine` at each join date
- JoinMarkerLabel: Pure CSS `:hover` on SVG `<g>` elements for tooltip show/hide (zero re-renders)
- BoxAnimationHero: SVG container now animates `y: -150` with opacity fade before content enters
- Content `startDelay` reduced from 4.55s to 4.095s; box slide begins at 3.495s

## Join Marker Details:
1. Faint dashed vertical lines (15% white opacity) with diamond markers at top
2. Diamond turns orange (#FF6B35) on hover
3. Tooltip: dark glass panel with avatar circles + usernames per join date
4. Markers fade in after the 3s chart reveal animation completes
5. 20px invisible hit area for easy hover targeting

Files Modified:
- src/app/(decoded)/decoded/components/InteractiveChart.tsx
- src/app/(decoded)/decoded/components/BoxAnimationHero.tsx
- src/app/(decoded)/decoded/page.tsx
